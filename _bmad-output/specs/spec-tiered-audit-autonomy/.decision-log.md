# Decision log — spec-tiered-audit-autonomy

## 2026-05-30 — create (from aligned in-conversation architecture)

Input was a rich, pre-aligned architecture brain dump (no source file) — extracted directly, no elicitation. Slug `tiered-audit-autonomy`. 8 capabilities; 4 companions (audit-tiers, autonomy-levels, architecture-diagrams, brownfield); diagrams routed to a companion per Spec Law rule 2.

**Self-validate.** Pass 1 (coherence): every CAP has intent + success; intents kept to WHAT (the one borderline — CAP-1 "from the tool log alone" — is the load-bearing distinction that defines the capability, retained deliberately); constraints all bend design (model choice, no-every-turn-Opus, non-recursion, measured-not-kudos autonomy, mandatory cap, deterministic-where-decidable, non-blocking audit); 6 explicit non-goals; success signal is demonstrable (delegation correctness + bounded per-tenant cost + declining audit frequency over time). Pass 2 (preservation): all load-bearing source claims landed — tiered audit (CAP-1/2/3 + audit-tiers.md), loop guard (CAP-4), L1–L4 earned autonomy (CAP-5 + autonomy-levels.md), cost-via-learning + recurrence layer (CAP-7 + diagrams), spend-cap circuit breaker (CAP-6), one-audited-path/team-digest leak (CAP-8), don't-downgrade-Iris and prevention-over-repair (Why + Constraints), current state and agreed rollout (brownfield.md). No load-bearing claim dropped; only conversational framing was left out.

**Open questions** carried into SPEC.md (4): daily-cap default per plan + whether it throttles generation; autonomy window/threshold; Tier-2 one-call-vs-two; Tier-0 advisory-vs-blocking first increment. These need a human decision before the relevant capabilities are built; they do not block CAP-6/CAP-1 (the first two rollout steps).

## 2026-05-30 — decisions from Devon + real cost data

- **Tier-0 = BLOCKING** (Devon): a Tier-0 violation forces a bounded revision, not log-only. Moved from Open Questions to Decisions in SPEC.md; audit-tiers.md Tier-0 "Blocking?" updated.
- **Spend-cap default grounded in chat_runs** (queried live via Supabase REST, service-role key): lifetime $23.25 / 280 runs; daily $0.37–$2.67; typical $1–2.50; busiest $2.67 (May 23); ~99% whatsapp/Sonnet (Iris generation, not audit). Proposed default **$5/tenant/day** (~2× busiest, ~$150/mo ceiling), Tier-2-disables-first, owner replies never hard-blocked. Devon was "not entirely sure" on the value — kept as Open Question with this concrete proposal to react to.
- **Observation:** debug-tool runs use `persist=false`, which skips `recordChatRun`, so debug spend is real but absent from chat_runs (explains low May-29 figure). Intended for the cap (meter real owner traffic only); flagged so cost observability isn't mistaken for complete.
- Remaining open: autonomy window/threshold; Tier-2 one-call-vs-two.
