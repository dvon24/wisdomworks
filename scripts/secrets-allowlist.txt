# Allowlist for the pre-commit secrets scanner.
# One SHA-256 hash per line. Lines starting with # are comments.
# The hash is the SHA-256 of the full line containing the false positive.
# The scanner prints the hash on each hit so you can paste it here.
#
# DO NOT use this file to silence real secrets. The scanner errs toward
# precision (vendor-specific patterns only) so false positives should be
# rare. When they happen, the safer fix is usually to rewrite the
# problematic line.
